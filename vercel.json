{
  "buildCommand": "cd 'project 12' && npm install && npm run build",
  "outputDirectory": "project 12/dist",
  "installCommand": "cd 'project 12' && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

